self.addEventListener("push",event=>{let d={};try{d=event.data.json()}catch{};event.waitUntil(self.registration.showNotification(d.title||"Restock Alert",{body:d.body||"Product status changed",image:d.image||undefined,tag:d.tag||"restock",requireInteraction:d.status==="in_stock",data:{url:d.url||"/"}}))});self.addEventListener("notificationclick",event=>{event.notification.close();event.waitUntil(clients.openWindow(event.notification.data?.url||"/"))});
